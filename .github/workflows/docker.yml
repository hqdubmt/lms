name: Build & Push Docker (multi-arch)
# v1.5.0

on:
  push:
    branches: [master]
    paths:
      - 'apps/api/**'
      - 'apps/web/**'
      - 'docker/**'
      - 'Dockerfile'
      - '.github/workflows/docker.yml'

jobs:
  build-push:
    name: Build linux/amd64 + linux/arm64
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up QEMU (for arm64 emulation)
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Get short commit SHA
        id: sha
        run: echo "short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      # ── API ──────────────────────────────────────────────────────────────
      - name: Build & push lms-api
        uses: docker/build-push-action@v5
        with:
          context: apps/api
          file: apps/api/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            hqdu/lms-api:latest
            hqdu/lms-api:${{ steps.sha.outputs.short }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # ── WEB ──────────────────────────────────────────────────────────────
      - name: Build & push lms-web
        uses: docker/build-push-action@v5
        with:
          context: .
          file: apps/web/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            hqdu/lms-web:latest
            hqdu/lms-web:${{ steps.sha.outputs.short }}
          build-args: |
            NEXT_PUBLIC_API_URL=${{ secrets.NEXT_PUBLIC_API_URL }}
            NEXT_PUBLIC_SOCKET_URL=${{ secrets.NEXT_PUBLIC_SOCKET_URL }}
            NEXT_PUBLIC_APP_URL=${{ secrets.NEXT_PUBLIC_APP_URL }}
            NEXT_PUBLIC_APP_NAME=${{ secrets.NEXT_PUBLIC_APP_NAME || 'MasterLMS' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # ── AUTO-DEPLOY ───────────────────────────────────────────────────────────
      - name: Deploy to server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_KEY }}
          script: |
            cd ${{ secrets.DEPLOY_PATH }}
            docker compose pull api web worker
            docker compose up -d --no-deps api web worker nginx
            docker image prune -f
